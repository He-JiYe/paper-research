# Paper Research 配置示例
# 复制为 config/config.yaml 后填写自己的值。
# 安全建议：所有密钥均支持 ${ENV_VAR} 占位符，优先使用环境变量，避免明文写入本文件。
#   Windows PowerShell 设置环境变量（持久化，需新开终端生效）：
#     setx DEEPSEEK_API_KEY "sk-xxxx"
#     setx ZOTERO_API_KEY "xxxx"
#     setx ZOTERO_LIBRARY_ID "12345678"
#     setx EMAIL_PASSWORD "smtp-auth-code"

llm:
  provider: deepseek
  model: deepseek-chat            # 如使用其他模型请确认 API 侧模型名有效
  api_key: ${DEEPSEEK_API_KEY}
  api_base: https://api.deepseek.com
  temperature: 0.3
  max_tokens: 2000

fetch:
  source: arxiv
  max_concurrent_requests: 3      # arxiv 官方建议 <=1 请求/3 秒，勿调大
  lookback_days: 3
  max_results: 20
  sort_by: lastUpdatedDate

keywords:
  - keyword: test-time adaptation
    arxiv_cats: [cs.CV, cs.LG]
    active: true
  - keyword: large language model
    arxiv_cats: [cs.CL]
    active: true

notification:
  email:
    enabled: false
    smtp_host: smtp.qq.com
    smtp_port: 465
    username: your-email@qq.com
    password: ${EMAIL_PASSWORD}
    from: your-email@qq.com
    to: your-email@qq.com

scheduler:
  enabled: true
  fetch_time: "08:30"
  timezone: Asia/Shanghai
  catch_up_on_start: true          # serve 启动时若今天错过抓取则自动补抓

server:
  host: 127.0.0.1                  # 不要改为 0.0.0.0，除非你知道自己在做什么
  port: 8899

zotero:
  api_key: ${ZOTERO_API_KEY}
  library_id: ${ZOTERO_LIBRARY_ID}
  library_type: user
